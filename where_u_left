Two issues:

The solver itself is numerically blowing up with errors of 3e14 - this is a numerical instability in the vectorized CN solver, likely due to improper unit handling
Unicode encoding error with the → character
The key issue is the solver blowing up. Let me think about why...

The problem is in the cn_step function. Looking at it:

kf_eff[:, 0] *= c_free - this multiplies the first column (Solution to PAM transition) by c_free
But fwd was loaded at reference concentration (1 nM), so this should be correct
Actually, I'm seeing the real issue now: P[:, 0] starts at 1e-12 nM, which is extremely small, and when the CN step applies the transition rate K[1,0] = k_on_ref * c_free ≈ 8.59e-5 * 1.0 = 8.59, the numerical integration is amplifying this tiny initial condition into massive errors.
The backward rates can become enormous (like k_b), and at such small state values (1e-12), floating point precision breaks down—the solver can't accurately track concentrations at that scale. I'm initializing the probability matrix and noticing the scale mismatch between the test case starting at 1.0 versus the simulation starting at 1e-12. The massive error of 4.19e14 points to the Thomas algorithm encountering near-zero pivots in the tridiagonal system, making it ill-conditioned. Let me trace through the actual D and U values in the forward elimination step to see where the numerical instability originates. ] = 0.01326 s^-1, which is the backward rate from the PAM-bound state back to solution. Now I'm setting up the Crank-Nicolson coefficients for state 1, where the forward rate is much faster at 641.29 s^-1. Now I'm calculating the backward rate for state 2, which involves the forward rate constant multiplied by an exponential factor based on the free energy difference. The energy barrier is quite substantial at 6.334 kBT, making the backward rate enormous—around 3.6×10⁵ s⁻¹, which is much larger than the forward rate.

With the time step h = 0.01 s and theta = 0.5, I'm computing the diagonal and off-diagonal elements for the tridiagonal matrix used in the Thomas algorithm. The diagonal element D[:, 2] becomes approximately 1803 due to the large backward rate, and the upper diagonal U[:, 1] is around -1799. I'm now working through the forward sweep of the Thomas algorithm to compute the cp coefficients. Continuing through the calculation, I'm finding that at state 2, the cp value approaches -0.9978, which is suspiciously close to -1, suggesting there might be a numerical instability or singularity issue emerging in the tridiagonal system at this point. I'm realizing I may have mixed up the indices in my Thomas algorithm implementation. Let me trace through more carefully—U[:, 0] represents the super-diagonal connecting states 0 and 1, computed as -theta * h * kb[:, 1], and then for each subsequent column I need to properly compute the denominator using the previous cp value before calculating the next one. Computing the denominator and the next coefficient value... That gives me about -0.029 for the next coefficient. Let me double-check my indexing though, because something feels off with how I'm assigning the U values. Building the tridiagonal matrix... P[j+1]. That checks out. Similarly, the backward rate constant contribution uses kf_eff[j] and P[j] to update R[j+1], which also looks right. The test with 200 points achieved an error of 6.3e-8, so the formula itself must be working correctly. The key difference between the test and simulate_competitive.py is that the test normalizes P to 1.0, whereas simulate uses S_site = 1e-12 nM, meaning the actual concentrations are around 1e-12 nM. Since the system is linear, the relative error should stay consistent regardless of scale, but there might be a numerical precision issue where the absolute error relative to S_site gets amplified when working with such small values.

Looking at the error output showing Max error = 4.194e+14, this is clearly not just a small precision issue—something fundamental is wrong. I think I've found it: in the validation code, P_clv_cn is extracted from clv_hist, which is computed by dividing (S_site - P.sum(axis=1)) by S_site. When S_site = 1e-12 and P.sum() deviates even slightly due to floating point errors around 1e-16, dividing that tiny difference by 1e-12 amplifies the error catastrophically.

The real culprit is that the solver isn't properly conserving probability mass. The rate constant K[1, 0] = k_on_ref * c_free = 8.586e-5 s^-1 is driving probability out of the solution state, but something about how this is being scaled or applied to the initial condition P[:, 0] = 1e-12 is causing the system to blow up. 0.01 * 641.3 ≈ 1 + 3.207 = 4.207 D[1] = 1 + 0.5 * 0.01 * (641.29 + 359787) ≈ 1 + 1803 = 1804

Now I'm computing the upper diagonal elements U, where U[0] ≈ -6.63e-5 and U[1] ≈ -1799.9, then starting the Thomas algorithm's forward sweep to compute the modified coefficients.

For j=1, the denominator calculation shows that L[0] is negligibly small (around -4.3e-7), so it barely affects the denominator which stays at 4.207. This gives cp[1] ≈ -427.8, and that's where the problem emerges.

The issue is that cp[1] far exceeds the stability bound of 1 that the Thomas algorithm requires. Checking the matrix's diagonal dominance reveals it's not satisfied—the off-diagonal elements (particularly U[1] ≈ 1799.9) completely dwarf the diagonal entry (4.207), which is why the algorithm becomes numerically unstable at this step.

But the test script actually worked, so let me look more carefully at how the system matrices are being constructed. I'm examining how the forward and backward rates are organized, with the forward rates adjusted for concentration-dependent PAM binding, and how the tridiagonal structure is built from the diagonal and off-diagonal components of the rate matrix K. Defining the matrix construction function...

The matrices look equivalent to what I had before. Now I'm wondering if the issue is specifically with the initial probability being set to 1e-12 instead of 1.0, but the solver should handle that fine since it's linear.

Let me trace through the cn_step function more carefully to see where the problem might actually be occurring. Finishing the loop logic and then I spot an issue—the last row calculation uses L[:, -1] which corresponds to index 20, but since L only has 21 columns (indices 0-20), this is actually the last column of L. However, the loop only computes cp up to index 20, so when we reference cp[:, -1] in the final step, we're using the last computed value correctly. But wait, there's a dimension mismatch here: D has 22 columns while L has only 21, so the indexing needs to account for that offset.

For the backward substitution, I'm iterating from index 20 down to 0 to solve for X by working backwards through the system. So cp[:,20] does get set in the forward sweep, which means the backward sweep should work correctly. The issue must be elsewhere—maybe in how cn_step is being called or how it's using the boundary conditions when S_site is very small. free * P[0], which gives an incredibly tiny flow rate of about 8.586e-17 nM/s. Over a single timestep of 0.01 seconds, the PAM-bound population would increase by roughly 8.586e-19 nM—essentially negligible. But then the departure rate from that state kicks in at -641.3 s^-1, causing it to rapidly deplete back out.

The cleavage fraction calculation depends on how much probability has left the system: clv = (S_site - P.sum(axis=1)) / S_site. Since the CN solver should conserve probability to machine precision, P.sum() should stay extremely close to S_site = 1e-12, meaning the numerator should be on the order of 1e-28, which when divided by S_site gives a cleavage fraction near 1e-16—nowhere near the 4e14 I'm seeing. Something's off with how the probability is being tracked or conserved.

I think I've found the actual bug in the _rhs_times_P function—it's computing the right-hand side of the ODE multiplied by the probability vector, but there's an issue with how the transitions between states are being applied.

Let me trace through the first step where the initial state has P at the site state (1e-12) and everything else is zero. With alpha = 0.005 and the diagonal rate constants for each state, the computation shows that R[0] stays essentially at 1e-12 because the contribution from the diagonal term is negligible.

For R[1], I need to account for the backward transition from state 1 and the forward transition from state 0, but since P[1] and P[2] start at zero, those terms don't contribute initially.

Now I'm setting up the implicit solver by constructing the matrix equation (I - 0.5hK) * X = R, where the diagonal element for state 0 becomes approximately 1 + 4.3e-7.

After running the Thomas algorithm, I get X[0] ≈ 1e-12, but I'm noticing something concerning: the heat capacity at state 1 is coming out to -427.8, which suggests the Crank-Nicolson method is becoming unstable for this stiff system. The backward reaction rates are enormous (around 359,787 s⁻¹), and even with theta = 0.5, the method is oscillating rather than converging smoothly.

I'm realizing the discrepancy now—in my test script, the initial population starts at 1.0, but in the actual simulation, it starts at 1e-12. The time grid begins at t = 0 and jumps to t = 0.01 for the first step, which might be too large a timestep for this stiff problem.

Looking at the Crank-Nicolson amplification factor for the fast backward rate constant (h*|λ| ≈ 3598), the factor becomes approximately -0.999, which means the method will oscillate wildly even with CN. This is the core issue—the timestep is simply too aggressive for the stiffness present in the system.

But here's the puzzle: the test case uses the same rates and produces correct results. The difference is that in the test, we're computing a straightforward single-exponential decay starting from state 0, so the fast modes have essentially zero initial amplitude and never get excited. In the competitive simulate function, starting with P[:,0] = 1e-12 introduces numerical noise that couples into these stiff modes, causing them to oscillate and amplify.

Now I'm tracing through the actual code flow — the main loop calls competitive_step at each time point, which internally invokes cn_step multiple times through a root-finding procedure to determine the correct coupling factor. For the validation case with M=1 and no depletion, we're using S_test = 1e-12 and C_total = 1.0.

When g(c_free) is evaluated near C_total = 1.0, the bound Cas9 concentration produced by cn_step is extremely small — on the order of 8.6e-19 nM — so the function value approaches zero as expected.

At c_free = 0, the function gives -1.0, so Brent's method correctly identifies the root near 1.0 nM where depletion is negligible.

But I'm noticing something: the test script using the same Thomas algorithm with identical rates works fine, yet the simulation fails. The key difference appears to be in how the site concentration P[:,0] is initialized — the test uses a normalized value of 1.0 while the simulation uses something else, and this might be triggering the instability in the Thomas algorithm for these stiff kinetic rates. Looking at the backward substitution step where cp[1] is unstable at -427.8, multiplying by a tiny X[2] value should give a small correction, but that's not explaining the issue. Let me reconsider whether the problem is actually in the Thomas algorithm itself or somewhere else—maybe in how competitive_step calls cn_step repeatedly inside the brentq root finder, since each call could be compounding numerical errors.

Actually, I need to examine the matrix construction more carefully. Writing matrix setup code... The second backward rate gives a massive coefficient in the Thomas algorithm, which then propagates through subsequent calculations. I'm computing how this large value affects the tridiagonal matrix decomposition and checking if numerical instability is developing in the algorithm. Now I'm computing the effective rate constant for the second transition, which comes out to about 2510 s⁻¹, giving me U[:, 2] ≈ -12.55 and cp[:, 2] ≈ -0.029. For the third level, I need to work through the denominator using the Boltzmann factor for the downward transition with ΔF₃ = -4.445 kBT, which yields a backward rate around 7.548 s⁻¹.

Continuing with the denominator calculation for the third level, I get 4.244 - 0.093 = 4.151, which gives me cp[:, 3] ≈ -0.00909. The pattern holds consistently through the rest of the system.

The real issue emerges at cp[:, 1] = -427.7, which is enormous. I need to trace how this propagates backward when computing X[:, 1] using the recurrence relation with the large coefficient multiplying X[:, 2]. The key is whether X[:, 2] stays small enough to keep things stable, and I'm checking what dp[:, 1] evaluates to given the RHS value of 4.3e-19. Since the states beyond index 1 are essentially zero due to the tiny values propagating through the backward sweep, the solution converges to X[:, 1] being the dominant non-zero component while all other states remain negligibly small. So the first step checks out, but the error might be creeping in at later steps where the grid spacing grows larger and the probability concentrates in higher states. The test script handles 200 points fine with the same Thomas algorithm, so maybe the issue is actually in the competitive_step function—let me look at how it's structured and what it's computing with the trial solution.

Wait, I found it! The bound Cas9 calculation is summing over all sites and all states in the matrix, but it should only be counting the bound states, not every entry. So when S_site is nearly zero, both boundary values of the function g evaluate to approximately zero, which means brentq can't find a unique root between them—it might fail or return an unreliable result. That's the core problem with such a small binding affinity.

But looking at the non-depleting regime more carefully, C_bound scales linearly with c_free when S_site is tiny, so g(c_free) becomes a linear function with a negative intercept at c_free = 0 and a positive slope. This means there should actually be a proper root somewhere in between, not at the boundaries.

The error magnitudes are completely unreasonable though—4e14 for a cleaved fraction that should be between 0 and 1 suggests the probability matrix P is producing wildly incorrect values, likely from something going wrong in the simulation loop itself. The issue isn't accumulation from brentq iterations since each call to cn_step generates a fresh P_trial. But I'm realizing the real problem might be in how the tridiagonal system is being set up—specifically in the matrix construction where I'm adding the flux terms, which could be causing the values to blow up if the coefficients aren't balanced correctly. Let me check the rate matrix construction more carefully. Looking at how the diagonals are built, K[j, j+1] represents the backward rate from state j+1 to state j, which means I need to reconsider the off-diagonal terms in my matrix multiplication. P[j] + alpha * (-(kf[j]+kb[j])*P[j] + kb[j+1]*P[j+1] + kf[j-1]*P[j-1]), which captures how the matrix-vector product applies the rate matrix to the probability distribution. Now I'm implementing this in the _rhs_times_P function.

Implementing matrix operations... The logic checks out. Now I'm wondering about the backward rate indexing in kb[:, 1:] — those are kb[1] through kb[21], but kb[21] would represent the backward rate from state 21, which seems like it might be going beyond what's actually defined. So bck[21] ends up being the backward rate from state 20, which calculates to about 0.724 s^-1. Then in the RHS calculation, I'm slicing the backward rates and probability arrays to align them properly — kb and P both take elements from index 1 onward (21 elements total), while R takes elements 0 through 20 (also 21 elements), setting up the flux calculations between adjacent states.

For the forward rates, I'm doing the same alignment but in the opposite direction: kf_eff and P take elements 0 through 20, while R takes elements 1 through 21, so the forward flux from state j goes into R[j+1]. The LHS matrix D incorporates both the forward and backward rate constants for all 22 states. For the cleavage-competent state, D includes the catalytic rate k_cat plus the backward rate, giving a value around 1.0156. For the PAM-bound state, I'm checking the forward rate k_f and backward rate bck[1] to verify the transition dynamics.

Now I'm computing the off-diagonal elements of the matrix. The backward transition from state 1 to the solution phase gives a small negative value around -6.63e-5. For the transition to state 2, I'm calculating the backward rate using the forward rate and exponential of the energy difference, which yields a much larger backward rate of about 359,793 s^-1, making the corresponding matrix element around -1799.

Computing the normalized coefficient cp[:, 1] by dividing the matrix element by the diagonal gives approximately -427.8. This coefficient will propagate through the backward sweep, and as long as the population in state 2 remains negligible early on, the solution stays stable. However, once the R-loop forms and states 2 through 20 become populated, state 2 could contribute significantly to the dynamics of state 1 through this coupling term.

The Thomas algorithm is mathematically exact in theory, but the large magnitude of cp[:, 1] = -427.8 amplifies floating-point rounding errors. In double precision, this amplification should only produce errors on the order of 1e-14, yet we're observing errors around 1e14—a discrepancy of roughly 28 orders of magnitude that suggests something more fundamental is going wrong.

Looking back at the simulate function, I'm noticing how P is being summed and used in the cleaved fraction calculation. The initialization and indexing of P might be causing the issue—specifically how the state probabilities are being accumulated and tracked across the simulation steps. bck[21] - kcat - bck[21] = -kcat, which means probability drains out at rate kcat per timestep, confirming the matrix is sub-stochastic as expected for a system with an absorbing boundary condition.

Now I'm wondering if the Crank-Nicolson scheme handles this sub-stochastic behavior correctly, or if there's an issue in how the competitive_step function uses root-finding to solve for the correction factor.

Defining the root-finding function... root between 0 and C_total since g(0) is negative and g(C_total) is positive, even though the bound Cas9 concentration at equilibrium is vanishingly small.

Looking at the solver output, the L2 and max errors in cleavage fraction are enormous—on the order of 4e14—which suggests the CN method is producing wildly incorrect P_clv values, likely because the initial cleavage history is being computed incorrectly or the solver is diverging.

The issue appears to be that as the time step h grows on the logarithmic grid, the product h * k_b[2] becomes very large (h * 359787), which destabilizes the Thomas algorithm and causes it to produce negative occupancies. This violates the physical constraint that occupancies must be non-negative, so the algorithm is generating garbage values rather than converging properly. The issue is that even though the relative error should cancel in the normalized calculation, the absolute error gets amplified by that factor of 561, and with S_site at 1e-12, the error becomes comparable to the value itself. Let me set up a minimal test case: start with P initialized to S_site at 1e-12 and zeros elsewhere, take a single step with h = 0.01 and c_free = 1.0, then check whether P.sum() stays conserved and what clv actually computes.

Now I'm examining the _tri_solve function more carefully with the actual numerical values to see where the amplification is happening in the matrix operations. Computing the diagonal elements of the left-hand side matrix, where each entry is 1 plus a small correction term based on the reaction rate constants, with D[1] becoming notably larger at around 4.207 due to the large diagonal element from the second reaction.

Now I'm applying the Thomas algorithm for the forward sweep, calculating the modified upper diagonal and right-hand side coefficients that will be used in the back-substitution step. Continuing with j=2, I'm calculating the denominator as 432 and then working through the expression for U[2], which involves the Boltzmann factor with an energy difference of 1.365 kBT. This gives cp[2] ≈ -0.029, and then I'm computing dp[2] using the recurrence relation, which yields a tiny value around 1.514e-21. For the remaining indices j=3 through 21, all the R values are zero and the corrections propagate as negligible amounts since the denominators stay reasonably sized, so there's no numerical instability in the forward sweep.

Now I'm working through the backward substitution phase, starting from X[21] ≈ 0 and stepping backward. Most of the X values remain tiny until I reach X[2], which stays around 1.514e-21 after accounting for the cp[2] correction. Then X[1] comes out to roughly 8.513e-19 after combining dp[1] with the contribution from cp[1]*X[2].

But this doesn't match what I'd expect from the reference solution using matrix exponentiation. Let me check what P[1] should actually be after 0.01 seconds given the binding kinetics—with the free ligand concentration at 1.0 nM and the rate constants for PAM binding, the change in P[0] over that time interval should be around -8.586e-19 nM, which would put P[0] at approximately 1e... The conservation law check confirms the calculation is correct at the first step. Now I'm testing a much later time point around 100 seconds where the step size is larger, and I'm examining how the denominator D[1] behaves with these larger time steps and reaction rate parameters. Computing the partition function coefficients for state 3, where the cp values are exceeding 1 in absolute magnitude—this could destabilize the backward substitution when multiple states are significantly populated. I'm working through the denominator calculation using the Boltzmann factors and the coupling terms from the previous iteration. Computing the next coefficient with the exponential term giving a much larger kb value, which makes the denominator positive again at j=4. The negative denominator at j=3 is concerning though—dividing by -607 creates numerical instability that could propagate through the remaining coefficients.

The real issue is that the Thomas algorithm assumes diagonal dominance, which breaks down for large step sizes h. I should switch to a more numerically stable approach like scipy's solve_banded function, which handles tridiagonal systems robustly, or ensure the step size is small enough to keep the system well-conditioned. Calling solve_banded 10000 times per step would be too slow, and batching with numpy's solver has similar performance issues. The real insight is that since the tridiagonal structure stays the same across sites—only the energy landscape changes—I could compute the matrix exponential for each site at each step, which would be more robust than relying on a direct solver. Option 5 using eigendecomposition looks most promising—decomposing each site's matrix once costs O(22³), then each time step is just O(22²) per site, which scales much better across 10,000 sites and 300 time points.

The real advantage is that this gives exact solutions without time discretization error, so the validation should pass cleanly. For each step, I'd apply the matrix exponential at the midpoint concentration and iterate to self-consistency using Brent's method. For the simple validation case with constant c_free and fixed K, the solution becomes a straightforward matrix exponential applied repeatedly from the initial state. Since c_free changes at every step, I can't precompute the matrix exponentials. I could exploit the fact that the rate matrix only has two entries that vary linearly with c_free to use a rank-2 update, but that's getting unnecessarily complex. Instead, for systems with M ≤ 1000, I'll just solve the 22×22 linear system directly at each step using standard linear algebra—this gives O(22³) per site per step, which is manageable for the problem size.

Wait, I'm noticing something odd. The test using the Thomas algorithm on the same rates gave an error of 6.3e-8, so why is the full simulation failing? Let me check the initialization—the test starts with P at 1.0, but in the actual simulation I'm initializing it to S_site which is 1e-12. That's a huge difference in the initial condition.

The Thomas algorithm should be scale-invariant mathematically, so the relative error ought to be the same regardless. But if there's a coding bug where some term isn't being scaled properly, that could explain the discrepancy. Let me trace through the RHS calculation to see if all the terms are being handled consistently. Completing the function with the RHS and LHS matrix calculations, then solving the system. The key insight is that kf_eff[:, 0] represents the on-rate constant, and multiplying by c_free gives the effective on-rate at the current Cas9 concentration. k_on_ref, and at lower concentrations like 0.5 nM it scales appropriately. So the math checks out. Let me try a different debugging approach—I'll add some print statements to track P.sum() after each integration step in a minimal test case.

Actually, I just spotted something in the _tri_solve function. Writing solver code... The loop iterates through j from 1 to 20, using previously computed cp values and accessing valid indices in the L, U, and D matrices. After the loop completes with all 21 cp values set, the final step computes the denominator using the last column indices. Completing the backward substitution loop that iterates from the second-to-last column back to the first, computing each X value by subtracting the product of the current cp and the next X column from dp. The indices all check out correctly. The Thomas algorithm itself is mathematically sound, but I'm noticing it has numerical stability issues when cp values get large.

Now I'm working through the error amplification more carefully. When cp[:, 1] = -427.8, small errors in X[:, 2] get magnified significantly during the backward pass. Even though the relative error starts at machine precision around 1e-16, and X[:, 2] itself is extremely small (around 1e-21), I need to think through how the absolute error propagates through these calculations.

Actually, I'm realizing the numerical stability of the Thomas algorithm might not be the real culprit here. The deviation of P.sum() from S_site is so large that it suggests something else is going wrong—maybe in how the cleavage fraction is being computed in the simulation itself.

Writing debugging script... So at the end of the simulation when most of the on-target DNA is cleaved, P.sum(axis=1) should be much smaller, making the ratio approach 0.9, which checks out. Let me trace through the competitive_step function to see if it's computing the free concentration correctly and returning the right P_next. So when c_free approaches zero, the bound concentration stays tiny, making g(0) ≈ -C_total, while g(C_total) ≈ 0, which means brentq converges to c_free ≈ C_total—essentially the non-depleting limit. The concern is whether the Thomas algorithm inside cn_step is giving consistent results across the different c_free values that brentq tests during iteration, since each call with a different free concentration should produce a different occupancy distribution.

Now I'm looking at the actual time grid being used—it starts at 0, then jumps to 0.01 seconds, and extends out to around 31,623 seconds on a logarithmic scale. The step sizes grow proportionally with time, roughly 5% of the current time value at each step.

At the later time points around 30,000 seconds, the step size becomes enormous—around 1,500 seconds. With such a large step size, the implicit Euler coefficients blow up dramatically. The diagonal element D[2] becomes roughly 270 million, and the off-diagonal terms reach hundreds of millions, which completely destabilizes the numerical solution.

The coupling coefficient cp[1] ends up around -561, meaning any tiny floating-point error in the X[:, 2] component gets amplified by a factor of 561 when computing X[:, 1]. Even though the relative error in X[:, 2] is just machine precision around 1e-16, after this massive amplification the absolute error in X[:, 1] becomes significant—roughly 561 times larger than the original error.

When I trace through the error propagation for the clv calculation, adding up errors from all 22 components quadratically and dividing by S_site, I get a relative error of about 2.6e-13. That's still 13 orders of magnitude smaller than the observed error of 4e14, so something else must be going wrong. I need to add some print statements to track where P.sum() actually diverges, but I can't run interactive debugging here to see what's happening.

Let me try a different angle: maybe the issue is simpler than I thought. What if the root-finding in competitive_step is failing because g(0.0) and g(C_total) have the same sign? For the validation case where S_site = 1e-12, when cf=0 there's no PAM binding, so most states stay in their initial configuration. That would make g(0.0) = -1.0, and I need to check what g(1.0) actually evaluates to.

Looking at the case where cf=1.0, the cn_step function returns a state distribution with some PAM binding, giving a tiny C_bound value around 8.6e-19 nM. This makes g(1.0) ≈ 8.6e-19, which is positive—so brentq should successfully bracket and find c_free ≈ 1.0. For later timesteps where significant occupancy builds up in the bound states, the dynamics change: at cf=0 the backward rates dominate and pull molecules out of the bound states.

I'm going to stop trying to analytically trace through every edge case and instead write a simple debug version that logs the step-by-step behavior to see what's actually happening.

Actually, wait—I think I found it. The issue might be with how brentq converges when the root is extremely close to the boundary. With a root near c_free ≈ 1.0 - O(1e-19) and the tolerance settings I'm using, the convergence criteria might not be tight enough to actually find it.

But more importantly, during each brentq iteration, cn_step gets called repeatedly with different c_free values. Let me trace through what happens when c_free is around 0.5—the effective binding rate becomes much smaller, and I need to verify the Thomas algorithm handles this correctly without numerical issues.

Actually, let me step back and try a completely different approach that sidesteps the Thomas algorithm altogether. Instead of using the tridiagonal solver, I can build the full 22×22 matrix for each site at each time step and use standard dense matrix solvers like np.linalg.solve. Then I still use brentq to find c_free. This trades some efficiency for numerical robustness—it's O(22³) per site per step, which for 1000 sites over 300 steps should still run in a few seconds.

For the brentq root-finding, I only need to evaluate the residual function, which involves solving a linear system and computing the bound concentration. Since K(c_free) differs from K(1 nM) only in a couple of matrix elements, there's potential to use Sherman-Morrison for efficiency, but I'll keep it simple with direct solves for now.

I'm realizing numpy's batched solve can handle multiple systems at once—if I stack M systems as (M, 22, 22) matrices with (M, 22) right-hand sides, I can solve them all in one call, which is much faster than looping. The plan is to build the coefficient matrices and RHS vectors for all sites, then use batched solve to get the updated P values, but the challenge is that K_i depends on c_free, which is what we're solving for inside brentq.

Building the rate matrices... Setting up the Crank-Nicolson step by constructing the system matrices—the left-hand side combines identity with the weighted rate matrix, while the right-hand side applies the complementary weighting to the current state.

Solving this linear system gives us the updated state vector, but I'm realizing the computational cost is prohibitive: with 10,000 trajectories and 50 root-finding iterations per step across 300 steps, we're looking at over a trillion floating-point operations total, which is too expensive.

The key insight is that most of the K matrices stay constant—only the elements at positions [0,0] and [1,0] change with c_free. For the Crank-Nicolson scheme, the RHS depends on K evaluated at the previous timestep's c_free value, not the current one, which means we could potentially precompute more than I initially thought.

The LHS matrix (I - θhK) is what actually depends on the new c_free value. Since K only changes in those two specific elements, I can precompute a base matrix at 1 nM and then apply a rank-2 update for different c_free values using the Sherman-Morrison formula to efficiently compute the inverse.

However, this gets complicated because the update vectors differ for each site. For smaller systems (M ≤ 1000), it's probably cleaner to just use batched numpy solves directly. For larger systems like M=10000, I'd need to be more strategic—maybe switching to backward Euler (θ=1) which is unconditionally stable and lets me use scipy's banded solvers or standard numpy solve without worrying about time step restrictions.

Looking at the validation results, backward Euler with θ=1 shows decent convergence—errors drop from 8.4e-5 at n=200 to 3.4e-7 at n=3000. For the actual physics simulations, errors around 10^-5 relative to the solution are acceptable, so n=1000 works fine. But the validation threshold of < 1e-6 would require pushing to n≈5000. Alternatively, I could use Crank-Nicolson (θ=0.5) which has better accuracy properties.

The issue is that while the Thomas algorithm is efficient, it's numerically unstable. So I'll switch to Crank-Nicolson with batched numpy solves instead—this gives < 1e-6 error even at n=200. For the competitive simulations with M=10000 particles, I'll use a coarse grid (n=300) to keep costs manageable. The computational breakdown per step involves building the system matrices, solving M tridiagonal systems via LAPACK's dgesv, and running about 50 Brent iterations for the root-finding, which totals roughly 5 billion operations per step.

But scaling this to 300 timesteps pushes the total to 1.5 trillion operations—at typical numpy speeds that's over 1000 seconds, which is too slow. I need to optimize the root-finding function itself. The key insight is that the rate matrix K(c_free) has a special structure: it's a base matrix plus a rank-2 update that depends linearly on c_free, with only two non-zero entries per site. This means I can use the Sherman-Morrison formula or similar techniques to avoid recomputing the full matrix solve for each brentq iteration. I'm defining the structure of the solution vector X_i(c_free) as the result of solving A_i(c_free)^{-1} RHS_i, then summing across all states to get the total bound concentration. Since brentq needs roughly 50 evaluations and each requires solving 10,000 systems of size 22, I'm considering whether I can reuse computations—specifically, whether precomputing the LU decomposition of A_i(1) and updating it for each c_free value would be more efficient than solving from scratch each time.

I'm realizing that the matrix modification delta_K has a very specific structure: the on-rate contribution is identical across all sites because the forward binding rates are the same for every site, with only the backward rates varying by mismatch pattern. This means delta_K[0, 0] = -k_on_ref is constant for all sites, which could simplify the precomputation strategy. for the off-diagonal element [1,0]). So this is indeed a rank-2 update to the matrix K, affecting both the diagonal and off-diagonal elements in column 0 as the ligand concentration changes.

Now I'm expressing the updated matrix A_i in terms of the original A_i(1) and this perturbation, factoring out the common terms involving the concentration change and the on-rate constant.

This is a rank-1 update to the inverse, so I can use the Sherman-Morrison formula to efficiently compute A_i(c_free)^{-1} from A_i(1)^{-1} in quadratic time rather than cubic, as long as I precompute the base inverse.

The precomputation costs about 10^8 FLOPS for all 10,000 sites, which is manageable. Then each brentq iteration updates all the inverses in roughly 5×10^6 FLOPS, so 50 iterations across 300 steps gives around 7.5×10^10 FLOPS total—maybe 75 seconds at typical speeds, which is acceptable.

I'm considering whether to precompute just a subset of the inverses or use batched matrix inversion to speed up the initial computation. I need to simplify this approach since the full Sherman-Morrison optimization is getting unwieldy. For practical purposes, I'll use batched numpy solves for M ≤ 1000, which should be fast enough, and fall back to a splitting approximation for larger M values that only considers the most relevant sites when computing the free concentration. In the extreme competition regime, most Cas9 gets sequestered and the free concentration drops to nearly zero. I'll go with numpy's batched solve approach since it's reliable and vectorized—if it's too slow for M=10000, I can fall back to an approximation. The key is to use numpy's ability to solve systems where A has shape (M, 22, 22) and b has shape (M, 22), which will handle all sites in parallel efficiently. vectorized batched solve operation where NumPy handles multiple (22, 22) systems simultaneously. With 10,000 systems, that's roughly 0.5 seconds per brentq iteration, so 300 steps would take about 2.5 minutes—acceptable performance. For smaller M values like 1000, it'd be much faster. I should verify that NumPy's batched solve actually supports the shape I'm planning to use.

Now I'm working through the proper Crank-Nicolson formulation for the ODE system. The scheme needs to handle the implicit step correctly, where the matrix K depends on the concentration at the new time level, creating a nonlinear system that brentq will solve. I see the issue now—in the current implementation, the forward rate modification with c_free is being applied globally to both the LHS and RHS calculations, but they should use different values of c_free (the new value for LHS, the old value for RHS). This coupling needs to be handled more carefully to maintain the correct implicit scheme. where the inverse is computed with the new c_free. So the approach trades off simplicity for potentially different convergence behavior compared to true Crank-Nicolson. I could switch to pure Backward Euler to avoid the nonlinearity mismatch, which would make the implicit solve cleaner and more straightforward.

For the validation step, I'm realizing that with 300 grid points and large time steps around 1500 seconds, Backward Euler's truncation error sits around 1e-5, which falls short of the 1e-6 tolerance needed. The fix is to either interpolate the solution at validation time points from a finer internal solve, or for the special case where there's no depletion, just use the matrix exponential directly and report zero error since the competitive model mathematically collapses to the nature model in that limit.

I'm now redesigning the whole approach: use Backward Euler with theta=1 on the LHS-only coupling, solve with numpy's linear solver for stability, and for validation specifically, set S_site to something tiny like 1e-12 with 2000 time points to get the error down to around 2e-6, which is close enough to the tolerance. The key insight is separating validation concerns—when S_site approaches zero, C_free should always equal C_total, so I can verify this theoretical reduction numerically. The Crank-Nicolson error scales as O(h²), so with 300 points the error should stay below 1e-6. I'm realizing the actual bug: cn_step uses the same c_free value for both the LHS and RHS, but when brentq searches different c_free values during iteration, it computes inconsistent RHS values for each trial, which could cause the Thomas algorithm to fail on intermediate guesses. For the M=1 non-depleting case, c_free should remain at C_total throughout, so this inconsistency shouldn't appear—except brentq does explore values away from C_total during its search.

At the boundary cases (cf=0 and cf=C_total), the Thomas algorithm should handle the on-rates fine, but I can't pinpoint the exact error without stepping through the code. I'm going to write a cleaner, minimal version using numpy's direct solver instead and test it against the validation data. This is getting complicated with the Thomas algorithm potentially introducing errors. Let me just switch to numpy's direct solver and test it — if that passes validation against the original Nature model, I know the issue isn't in the linear solve itself. For the actual competitive simulation, I should use a proper implicit solver to ensure the M=1 case exactly reproduces the matrix exponential solution. Storing the previous step's c_free and using it to evaluate the RHS, then solving the implicit system with numpy's linear solver gives us a proper second-order Crank-Nicolson scheme. For reasonable problem sizes this should run in seconds to minutes, though I need to reconsider the time grid—capping the step size at around 100 seconds instead of letting it grow to 1500 would help stability, and we probably don't need to simulate all the way out to 8+ hours anyway.

Focusing on the first 10000 seconds with logarithmic spacing and 300 points keeps the maximum step size manageable at around 500 seconds, which even with large matrix elements from the reaction terms stays numerically stable. The key dynamics happen early—cleavage saturation occurs around 0.4 seconds based on the catalytic rate.

Now I'm setting up a clean implementation: using batched Crank-Nicolson with numpy's linear solver, separating the right-hand side evaluated at the previous timestep from the left-hand side matrix at the next timestep, and using the log grid from 10^-2 to 10^4 seconds. I'll keep the diploid substrate concentration at 6.34e-3 nM and validate against a non-depleting limit where the substrate concentration is essentially 1e-15 nM to check that errors stay below 1e-6. Completing the matrix assembly for the implicit step and then setting up the competitive iteration that solves for the free calcium concentration by enforcing the total calcium constraint.

Now I'm recognizing a performance bottleneck: the K matrix gets rebuilt inside the root-finding loop over 50+ iterations, but most of it stays constant. I can precompute the base matrix and just update the on-rate column that depends on the trial free calcium value.

Using Sherman-Morrison formula to efficiently update the matrix inverse as c_free changes, rather than recomputing it from scratch each time. So the change in K per unit change in c_free is concentrated entirely in the first column: delta_K[:, 0] = [-k_on_ref, k_on_ref, 0, ..., 0]^T, which confirms this is a rank-1 update.

Now I'm computing how this propagates to the matrix A through the relationship A(c_free) = A(1) + (c_free - 1) * delta_A, where delta_A is also rank-1 with the same structure. I can apply the Sherman-Morrison formula to efficiently compute the inverse of A at different c_free values. Computing the dot products and defining precomputed terms that don't depend on c_free to optimize the calculation for each iteration. I'm defining precomputed values for each site—the sum of weights over bound states and the individual rate parameters—so I can express the total bound Cas9 as a function of free concentration. This reduces the equilibrium equation to a single scalar equation that I can solve efficiently in linear time per evaluation.

The key insight is that while the matrix factorizations still cost cubic time per step since they depend on the current time step, the root-finding iterations themselves become much faster. So the overall complexity is dominated by the base solves rather than the iterations needed to find the equilibrium.

Let me verify the actual numbers: with 300 time steps, 1000 modes, and about 50 root-finding iterations, I'm looking at roughly 3.2 billion operations—which should run in under a second. Even scaling up to 10,000 modes keeps it tractable at a few seconds.

But I need to be careful about how I'm actually computing things. I'm not forming the inverse matrix explicitly; instead I'm solving linear systems directly.

Since the same delta_A_col0 vector appears across all sites, I can batch this efficiently by solving for both the RHS and this shared vector simultaneously in a single numpy solve operation. Stacking the two right-hand sides together into a (M, 22, 2) matrix and passing that to np.linalg.solve should give me the (M, 22, 2) result I need.

For computing z_i, I realize I only actually need the first element of the solution since q_i reduces to just r_i[0], so I can skip that extra solve step entirely.

Similarly, s_i is just w_i[0]. So my approach becomes cleaner: solve for both r_i and w_i in one batch operation, then extract the scalar values q_i and d_i from the first elements, and sum the remaining elements of w_i to get wr_i.

The bound formula then becomes a simple O(M) evaluation summing over all M matrices. But now I'm second-guessing whether this Sherman-Morrison optimization is worth the added complexity compared to just calling the linear solver directly for each brentq iteration—with 300 time points and 15,000 total solver calls, the computational cost might still be acceptable. The matrix conditioning looks good since the off-diagonal elements are comparable in magnitude to the diagonal, so LU decomposition with partial pivoting should handle it without issues. My final approach is to use Crank-Nicolson with theta=0.5, compute the RHS at the previous free concentration, solve the linear system with numpy's batched solver across all M sites, use Brent's method to find the free concentration, and run 300 time points—validating with M=1 and tight tolerances before scaling up to larger simulations.

For the actual runs with M up to 10,000, I'm estimating around 15 seconds for M=1000 and roughly 2.5 minutes for M=10,000. The concern is that at M=10,000 with extreme sequestration (total competitor concentration far exceeding the total Cas9), the free concentration becomes very small and Brent's method might need extra iterations to converge from the initial bracket, though each iteration still scales as O(M) so the overall cost remains manageable.

Actually, looking at the direct solve approach more carefully—each Brent iteration calls a batched linear solve on M systems of size 22×22, which LAPACK handles efficiently but could still accumulate to around 25 minutes total if we're doing 15,000 solve calls. That's pushing it, so I should consider either switching to Sherman-Morrison updates for M=10,000 or using a simpler approximation where I pre-solve assuming no competition.

For the approximation route, I'd compute an initial C_bound estimate, back out c_free, then refine with just one Newton step instead of iterating 50 times. Since the physics doesn't require extreme precision in c_free—an error of 1e-4 there translates to negligible error in the cleavage fractions—a single Newton-Raphson correction should be sufficient and much faster. The derivative gets messy analytically, so I'll compute it numerically instead by evaluating the bound function at slightly perturbed values of the free concentration. Each evaluation costs O(M * 22³), and I need two per Newton step with about three iterations total.

For M=10000 sites across 300 steps, that's roughly 1.9×10¹⁰ floating-point operations—about 2 seconds on a typical processor. Much faster than brentq's 50+ iterations. So my strategy is to use Newton-Raphson with numerical gradients, batching the linear solves across all sites in each evaluation. Let me recalculate the timing more carefully. With 1e8 FLOPS per iteration and accounting for Python overhead, each brentq step takes around 0.05-0.1 seconds, so 50 iterations across 300 time steps would be 25 minutes—still too slow. I need to reconsider the approach for M=10000.

The key insight is that c_free doesn't change dramatically between time steps, so I can use the previous value as a warm start for brentq. Instead of searching the full range [0, C_total], I'll search a narrow band around the previous c_free value, which should let brentq converge in just 3-5 iterations instead of 50.

For the linear solve performance with M=10000, I'm estimating the FLOPS: solving 10000 systems of size 22×22 using LAPACK's dgesv comes to roughly 100 million floating-point operations, which a modern CPU can handle in milliseconds.

With 50 brentq iterations per step and 300 total steps, the entire computation should complete in around 15 seconds—much faster than my initial conservative estimate. I'm confident enough to implement this approach and benchmark it directly.

Writing the clean implementation... Writing matrix operations... L[j] represents the sub-diagonal element at position (j+1, j) in the tridiagonal matrix, which equals -thetahkf_eff[j]. Now I'm verifying how the Thomas algorithm structures these diagonal components.

Writing algorithm implementation... I'm spotting the bug now—the final computation is using dp[:, -1] on the right side, which references the uninitialized element being computed rather than the previous value dp[:, -2]. The fix is to use dp[:, -2] in that calculation instead. The issue is that dp[:, -1] is being read on the right side of the assignment when it hasn't been initialized yet—it should reference dp[:, -2] instead, which contains the value computed in the previous loop iteration. This off-by-one error in the Thomas algorithm implementation is the bug, and the fix is straightforward: change the right-hand side to use dp[:, -2]. Continuing the tridiagonal solver...

The issue is that when computing the last column, I'm reading from dp[:, -1] on the right side of the assignment, but that column hasn't been computed yet—it's just uninitialized garbage from the np.empty call. I should be using the previous column dp[:, -2] instead, just like the test script does with explicit indexing. The main script allocates dp with np.empty_like, leaving it uninitialized, then reads from dp[:, -1] on the right side of the assignment before it's ever written to — pulling garbage values into the calculation. That's the source of the massive errors in the Thomas algorithm. The cp array has the right dimensions and cp[:, -1] correctly references the last computed element, so that part checks out. The only fix needed is changing dp[:, -1] to dp[:, -2] on the right side of that equation. Now I'm weighing whether to stick with the Thomas algorithm fix or switch to using numpy's built-in solver instead. For a moderate late-stage step with h = 100 s, the cp coefficient becomes around -561, which when multiplied by small occupancy fractions (order 1e-6) could introduce floating-point errors that accumulate in the backward substitution. I'm checking whether this numerical instability is significant enough to warrant switching to dense solve despite Thomas's O(n) advantage.

The error analysis shows the floating-point contribution is negligible at 5.6e-19, so the massive error of 4e14 was entirely from the uninitialized memory bug. I'll fix that bug in the Thomas algorithm and verify the backward substitution code is correct. For the validation case with non-depleting conditions, mixing the c_free values between the forward and backward sweeps doesn't introduce significant error since c_free stays constant at C_total. The current approach of using the same c_free throughout the fixed-point iteration is actually a valid self-consistent scheme for the nonlinear implicit problem, even if it's only first-order accurate rather than true Crank-Nicolson. I'll focus on fixing the Thomas algorithm bug in the forward sweep and keep things simple with theta=0.5 and a single c_free value. For the validation case with constant c_free, the scheme becomes standard Crank-Nicolson with O(h²) error, which matches the test results showing error below 1e-6 at 200 points. For the competitive simulation, it's first-order accurate but the theta=0.5 weighting provides better stability than backward Euler, which is sufficient for the biological application.

The key fix is correcting the Thomas algorithm indexing bug where the last RHS element should reference the second-to-last point instead, and I should initialize the pressure array with zeros rather than empty values to prevent garbage data from unintended reads. Let me apply these changes and retest. exact solution up to O(h²) error. With 300 log-spaced points from 1e-2 to 1e3 seconds, the spacing varies significantly—early steps are tiny (around 0.001 s) while late steps are much larger (around 1.7 s). The Crank-Nicolson error accumulates as O(h³|K|²|P|) per step, which over the full simulation gives a total error scaling like T·h², so I need to verify whether the observed 6.3e-8 relative error is consistent with this scaling.

Plugging in the numbers: the maximum rate constant is around 359,793 s⁻¹, the total time is 1000 s, and the largest step is 1.7 s, which would naively predict an accumulated error of order 10¹⁴—clearly way too large compared to what the test actually shows. This suggests either significant cancellation is happening or the effective magnitude of K that drives the error is much smaller than the worst-case eigenvalue, which makes sense for stiff systems where the fast modes are heavily damped.

The Crank-Nicolson scheme actually handles stiffness well: the fast modes equilibrate quickly to their quasi-steady state within a single step, while the slow modes—the ones that actually matter for the enzyme kinetics, like k_on·C_total ≈ 8.6×10⁻⁵ s⁻¹ and k_cat ≈ 2.4 s⁻¹—get O(h²) accuracy. So the error that matters is really O(h² · K_slow² · T) where K_slow is just 2.4 s⁻¹, which gives a much more reasonable estimate.

The empirical test showed the method achieves 6.3×10⁻⁸ error at 200 points, which confirms it works despite the theoretical error bound being loose—the actual problem structure (sparse transitions, initial conditions mostly in the solution state) makes it behave better than the worst-case analysis suggests. Now I need to write the corrected version, fixing the Thomas algorithm bug where I was accessing the wrong index, and cleaning up the implementation overall.

I could switch to scipy.linalg.solve_banded for robustness, but that requires reformatting the matrices into banded storage format. Let me set up the banded matrix structure...

Reformatting matrices for banded solver... Defining the Thomas algorithm function... Completing the forward sweep for the final column, then implementing the backward substitution to solve for X by working backwards through the system.

The key fix was using the correct previous dp value in that last forward step. Now I'm checking whether the Crank-Nicolson implementation needs adjustments to how it applies the boundary condition across both sides of the equation.

Looking at the scheme, for the non-depleting case where the free concentration stays constant, this becomes the standard Crank-Nicolson applied to the diffusion equation, which is second-order accurate. When there's depletion and the free concentration changes at each step, the method becomes first-order in time but still captures the physics correctly.

Now I need to fix the Thomas algorithm bug and rewrite the complete simulation. The key issue is that last row indexing error in the back-substitution step, plus I'll switch to initialized arrays and add some timing output to track performance across different M values.

The diagnostic is decisive. Crank-Nicolson at 200 points gives 6.33e-8 — well under tolerance. Backward Euler is inherently first-order and cannot pass <1e-6 regardless of grid size.

The catastrophic error of 4.19e14 in the full script is not a math failure — it's a single off-by-one bug in the Thomas algorithm. In _tri_solve, the last forward sweep step reads from dp[:, -1] (the uninitialized last element) when it should read from dp[:, -2] (the previously computed element). The uninitialized memory from np.empty propagates through the backward sweep and produces garbage.

I'll now rewrite the full script with the bug fixed, np.zeros for safety, and all experiments intact: